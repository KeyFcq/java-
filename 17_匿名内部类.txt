匿名内部类：
	在外部类的方法里面继承了其他类或者实现了其他接口的子类匿名对象( new Inter(){ public void method(){}};)
	
匿名内部类在开发中的应用：
  一个方法的参数为 接口 ，按之前使用方式为 创建这个接口的子类，然后使用多态的方式创建指向子类的父接口 Animal a = new Dog(); 最后将 a 传入 方法
  但是使用内部类后就可以 直接将 new Animal(){ show(){}} 当做参数传入 方法